# 📄🤖 Intelligent Enterprise Chatbot (RAG-Based)

A modern, lightweight **document-aware chatbot** built using **Flutter** (frontend) and **FastAPI + Python** (backend). Users can upload PDF documents, ask questions, and receive context-based answers generated by a **local free LLM (GPT-2 via Hugging Face)**.

---

## ✨ Features

- 📤 **Upload PDF files** directly from the Flutter application  
- 📚 **Extract text** from PDFs using PyPDF2  
- ❓ **Ask questions** about the uploaded document  
- 🤖 **GPT-2 LLM** generates context-aware answers (no API key required)  
- 💬 **Modern chat interface** with clean UI  
- ⚡ **FastAPI backend** for real-time response handling  

---

## 🛠️ Technologies Used

### 🎨 Frontend (Flutter)
- Flutter (Dart)  
- HTTP Client  
- Modern chat UI design  

### 🧠 Backend (Python)
- FastAPI  
- PyPDF2  
- Hugging Face Transformers (GPT-2)  

---

## 🔍 How the System Works

1. **📄 User uploads a PDF** from the Flutter app  
2. **🧩 Backend extracts text** from the PDF using PyPDF2  
3. **❓ User enters a question**  
4. **🤖 GPT-2 generates an answer** using the extracted text as context  
5. **💬 Answer is displayed** inside the chat interface  

---

## ▶️ Run the Backend (FastAPI)

```bash
cd ai_document_assistant_backend
pip install -r requirements.txt
uvicorn app:app --reload
```
## ▶️ Run the Flutter App
```bash
cd ai_document_assistant
flutter pub get
flutter run
```
